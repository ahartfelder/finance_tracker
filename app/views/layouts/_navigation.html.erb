<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow">
  <div class="container">
    <%= link_to 'Finance Tracker', root_path, class:"navbar-brand font-weight-bold" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <li class="nav-item <%= 'active' if request.path == my_portfolio_path %>">
            <%= link_to 'My Portfolio', my_portfolio_path, class:"nav-link" %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-expanded="false">
              Profile
            </a>
            <div class="dropdown-menu shadow" aria-labelledby="navbarDropdown">
              <%= link_to ((fa_icon 'edit') + " Edit Profile"), edit_user_registration_path, class:"dropdown-item" %>
              <div class="dropdown-divider"></div>
              <%= link_to ((fa_icon 'trash') + " Delete User"), user_registration_path, method: :delete, data: { confirm: "Are you sure?" }, class:"dropdown-item text-danger" %>
            </div>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="nav-item m-1">
            <%= link_to ((fa_icon 'user') + " " + current_user.full_name), edit_user_registration_path, class:"nav-link active" %>
          </li>
          <li class="nav-item m-1">
            <%= link_to 'Logout', destroy_user_session_path, method: :delete, class:"btn btn-danger" %>
          </li>
        <% else %>
          <li class="nav-item m-1">
            <%= link_to 'Login', new_user_session_path, class:"btn btn-success" %>
          </li>
          <li class="nav-item m-1">
            <%= link_to 'Sign Up', new_user_registration_path, class:"btn btn-primary" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>